# Sections

Each section contains:

- `PLAN.md`: mutable, non-authoritative discussion history;
- `SSOT.md`: draft until its dedicated PR is merged by Anand;
- `units/`: learner-facing units created only after lock.

Do not create placeholder units. Work on one section and one unit at a time.
